#################
#### GERMANY ####
#################

GER_Volkersintegration = {
		GER_core_danzig = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes ##put this line and next one under if statement to lock if not controlled
 		targets = { state = 85 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_danzig_or_war
			85 = {
				resistance < 25
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					POL = { has_completed_focus = POL_ban_the_nazi_party }
				}
				compliance > 60
			}
			if = {
				limit = {
					AND = {
						country_exists = POL
						POL = {owns_state = 85}
					}
				}
					POL = { has_capitulated = yes }
			}
		}
		visible = {
			85 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 0

		#EFFECTS

		complete_effect = {
			add_state_core = 85
		}
	}

	GER_core_gdynia = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 807 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_danzig_or_war
			807 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = POL
						POL = {owns_state = 807}
					}
				}
					POL = { has_capitulated = yes }
			}
		}
		visible = {
			807 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 807
			807 = { set_state_name = "Westpreußen"}
			set_province_name = {id = 279 name = "Bromberg"} #Bydgoszcz
			set_province_name = {id = 3295 name = "Thorn"} #Toruń
			set_province_name = {id = 9263 name = "Gotenhafen"} #Gdynia
		}
	}

	GER_core_poznan = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 86 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_danzig_or_war
			86 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = POL
						POL = {owns_state = 86}
					}
				}
					POL = { has_capitulated = yes }
			}
		}
		visible = {
			86 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 86
			86 = { set_state_name = "Posen"}
			hidden_effect = {
				set_province_name = {id = 6558 name = "Posen"}
			}
		}
	}

	GER_core_katowice = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 762 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_danzig_or_war
			762 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = POL
						POL = {owns_state = 762}
					}
				}
					POL = { has_capitulated = yes }
			}
		}
		visible = {
			762 = {
				AND = {
					is_controlled_by = ROOT
					NOT = {is_core_of = ROOT}
				}
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 762
			762 = { set_state_name = "Kattowitz"} #Katowice
			hidden_effect = {
				set_province_name = {id = 6464 name = "Kattowitz"}
			}
		}
	}

	GER_core_memel = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 188 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_demand_memel
			188 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = LIT
						LIT = {owns_state = 188}
					}
				}
					LIT = { has_capitulated = yes }
			}
		}
		visible = {
			has_completed_focus = GER_demand_memel
			188 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS
		complete_effect = {
			add_state_core = 188
		}
	}

	GER_core_tyrol = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 153 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_anschluss
			153 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = AUS
						AUS = {owns_state = 153}
					}
				}
					AUS = { has_capitulated = yes }
			}
		}
		visible = {
			has_completed_focus = GER_anschluss
			153 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS
		complete_effect = {
			add_state_core = 153
		}
	}

	GER_core_upper_austria = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 152 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_anschluss
			152 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = AUS
						AUS = {owns_state = 152}
					}
				}
					AUS = { has_capitulated = yes }
			}
		}
		visible = {
			has_completed_focus = GER_anschluss
			152 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS
		complete_effect = {
			add_state_core = 152
		}
	}

	GER_core_lower_austria = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 4 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_anschluss
			4 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = AUS
						AUS = {owns_state = 4}
					}
				}
					AUS = { has_capitulated = yes }
			}
		}
		visible = {
			has_completed_focus = GER_anschluss
			4 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS
		complete_effect = {
			add_state_core = 4
		}
	}

	GER_core_south_tyrol = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 39 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_south_tyrol_agreement
			39 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = GER
			}
			if = {
				limit = {
					AND = {
						country_exists = AUS
						AUS = {owns_state = 39}
					}
				}
				AUS = { has_capitulated = yes }
			}
		}
		visible = {
			has_completed_focus = GER_anschluss
			#ADD extra flag for no agreement
			39 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS
		complete_effect = {
			add_state_core = 39
		}
	}

	GER_core_sudetenland = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 69 }
		#CONDITIONS
		available = {
			has_completed_focus = GER_demand_sudetenland
			has_full_control_of_state = 69
			69 = {
				resistance < 25
				compliance > 60
			}
			if = {
				limit = {
					AND = {
						country_exists = POL
						CZE = {owns_state = 69}
					}
				}
					CZE = { has_capitulated = yes }
			}
		}
		visible = {
			has_completed_focus = GER_demand_sudetenland
			69 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS
		complete_effect = {
			add_state_core = 69
			set_province_name = {id = 11401 name = "Reichenberg"} #Liberec
			set_province_name = {id = 3585 name = "Karlsbad"} #Carlsbad
		}
	}

	GER_core_east_sudetenland = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 74 }

		#CONDITIONS
		available = {
			has_completed_focus = GER_demand_sudetenland
			has_full_control_of_state = 74
			74 = {
				resistance < 25
				compliance > 60
			}
			if = {
				limit = {
					AND = {
						country_exists = POL
						CZE = {owns_state = 74}
					}
				}
					CZE = { has_capitulated = yes }
			}
		}
		visible = {
			has_completed_focus = GER_demand_sudetenland
			74 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS
		complete_effect = {
			add_state_core = 74
			set_province_name = {id = 3583 name = "Ostrau"} #Ostrava
		}
	}

	GER_core_luxemburg = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 8 }
		#CONDITIONS (has the requirements of the old focus)
		available = {
			has_full_control_of_state = 8
			OR = {
				LUX = { has_capitulated = yes }
				LUX = {exists = no}
			}
		}
		visible = {
			8 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 8
			if = {
				limit = {
					NOT = { owns_state = 8 }
				}
				transfer_state = 8
			}
			LUX = {
				country_event = { id = wtt_germany.64 }
			}
		}
	}

	GER_core_alsace = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 28 }
		#CONDITIONS (has the requirements of the old focus)
		available = {
			has_full_control_of_state = 28
			OR = {
				owns_state = 28
				FRA = { has_capitulated = yes }
				FRA = { is_subject_of = ROOT }
				NOT = { #france doesn't exist
					any_country = {
						original_tag = FRA
						exists = yes
					}
				}
			}
		}
		visible = {
			28 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 28
			if = {
				limit = {
					NOT = { owns_state = 28 }
				}
				transfer_state = 28
			}
			28 = { set_state_name = "Elsass-Lothringen" }
			set_province_name = { id = 9503 name = "Strassburg" }
			set_province_name = { id = 678 name = "Mulhausen" }
			FRA = {
				country_event = { id = wtt_germany.63 }
			}
		}
	}
}

#################
#### HUNGARY ####
#################

HUN_integration = {
	HUN_core_Ruthenia = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 73 }
		#CONDITIONS
		available = {
			73 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = YUG
						YUG = {owns_state = 73}
					}
				}
					YUG = { has_capitulated = yes }
			}
		}
		visible = {
			73 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 73
		}
	}

	HUN_core_southSlovakia = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 664 }
		#CONDITIONS
		available = {
			664 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = YUG
						YUG = {owns_state = 664}
					}
				}
					YUG = { has_capitulated = yes }
			}
		}
		visible = {
			664 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 664
		}
	}

	HUN_core_NTransylvania = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 76 }
		#CONDITIONS
		available = {
			76 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = ROM
						ROM = {owns_state = 76}
					}
				}
					ROM = { has_capitulated = yes }
			}
		}
		visible = {
			76 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 76
		}
	}

	HUN_core_Transylvania = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 84 }
		#CONDITIONS
		available = {
			84 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = ROM
						ROM = {owns_state = 84}
					}
				}
					ROM = { has_capitulated = yes }
			}
		}
		visible = {
			84 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 84
		}
	}

	HUN_core_crisana = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 83 }
		#CONDITIONS
		available = {
			83 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = ROM
						ROM = {owns_state = 83}
					}
				}
					ROM = { has_capitulated = yes }
			}
		}
		visible = {
			83 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 83
		}
	}

	HUN_core_banat = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 82 }
		#CONDITIONS
		available = {
			82 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = ROM
						ROM = {owns_state = 82}
					}
				}
					ROM = { has_capitulated = yes }
			}
		}
		visible = {
			82 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 82
		}
	}

	HUN_core_WestBanat = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 764 }
		#CONDITIONS
		available = {
			764 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = YUG
						YUG = {owns_state = 764}
					}
				}
					YUG = { has_capitulated = yes }
			}
		}
		visible = {
			764 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 82
		}
	}

	HUN_core_Vojvodina = {
		icon = GFX_decision_icon_default
		on_map_mode = map_and_decisions_view
		state_target = yes
 		targets = { state = 45 }
		#CONDITIONS
		available = {
			45 = {
				resistance < 25
				compliance > 60
				is_fully_controlled_by = HUN
			}
			if = {
				limit = {
					AND = {
						country_exists = YUG
						YUG = {owns_state = 45}
					}
				}
					YUG = { has_capitulated = yes }
			}
		}
		visible = {
			45 = {
				NOT = {is_core_of = ROOT}
				is_controlled_by = ROOT
			}
		}

		fire_only_once = no #if yes, will appear once and never again after excecution
		#days_re_enable = 0 #invalid if fire_only_once = yes

		cost = 50

		#EFFECTS

		complete_effect = {
			add_state_core = 45
		}
	}
}